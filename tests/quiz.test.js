import test from'node:test';import assert from'node:assert/strict';import{questions,categories}from'../js/data.js';test('questions have four options and one valid answer',()=>{for(const q of questions){assert.equal(q.options.length,4);assert.ok(q.answer>=0&&q.answer<4);assert.ok(q.law&&q.article&&q.version)}});test('categories are unique',()=>assert.equal(new Set(categories.map(x=>x.id)).size,categories.length));
