import{categories,questions}from'./data.js';
const $=(s,r=document)=>r.querySelector(s);const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function card(c){return `<article class="card"><div class="eyebrow">${esc(c.icon)} სამართალი</div><h3>${esc(c.title)}</h3><p class="meta">${esc(c.desc)}</p><div class="card-footer"><span>${c.count} კითხვა</span><a class="btn" href="quiz.html?category=${c.id}">დაწყება</a></div></article>`}
if($('#categories .grid'))$('#categories .grid').innerHTML=categories.map(card).join('');
if($('#test-list')){const render=()=>{const q=$('#search').value.toLowerCase(),cat=$('#category').value,diff=$('#difficulty').value;const data=categories.filter(c=>(!cat||c.id===cat)&&(!q||c.title.includes(q)||c.desc.includes(q))&&(!diff||questions.some(x=>x.category===c.id&&x.difficulty===diff)));$('#test-list').innerHTML=data.length?data.map(card).join(''):'<p class="card">ტესტი ვერ მოიძებნა.</p>'};['input','change'].forEach(e=>$('.toolbar').addEventListener(e,render));render()}
if($('#leaderboard')){const rows=[['ნინო კაპანაძე','2 450','ექსპერტი'],['გიორგი მაისურაძე','2 210','ექსპერტი'],['ანა ლომიძე','1 980','მცოდნე'],['თქვენ','860','სტუდენტი']];$('#leaderboard').innerHTML=rows.map((r,i)=>`<tr><td>${i+1}</td><td>${r[0]}</td><td>${r[1]}</td><td><span class="badge">${r[2]}</span></td></tr>`).join('')}
if($('#certificate-form')){$('#certificate-form').onsubmit=async e=>{e.preventDefault();const code=$('#certificate-code').value.trim();const out=$('#certificate-result');out.textContent='მოწმდება…';try{const c=await window.App.getClient();if(!c){out.textContent=code==='DEMO-2026-80'?'ნამდვილი სერტიფიკატი — დემო მომხმარებელი, 85%, 12.07.2026':'სერტიფიკატი ვერ მოიძებნა.';return}const{data,error}=await c.rpc('verify_certificate',{p_code:code});if(error)throw error;out.textContent=data?.length?`ნამდვილია — ${data[0].full_name}, ${data[0].score_percent}%`:'სერტიფიკატი ვერ მოიძებნა.'}catch(e){out.textContent=window.App.friendlyError(e)}}}
